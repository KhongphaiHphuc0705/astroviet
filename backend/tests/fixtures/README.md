# Test Fixtures

Thư mục này chứa các dữ liệu mẫu (mock data, DTO templates) để sử dụng trong quá trình test, giúp các file test gọn gàng và tránh lặp lại cấu trúc (tránh magic string / magic objects).
Sẽ được sử dụng nhiều từ Sprint 1 (ví dụ: mock Birth Profile, mock User, mock Chart).
